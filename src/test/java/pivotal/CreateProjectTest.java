package pivotal;

import org.junit.Test;
import org.junit.Before;
import org.junit.After;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.Dimension;

public class CreateProjectTest {
  private WebDriver driver;
/*
  @Before
  public void setUp() {
    driver = new ChromeDriver();

  }
  @After
  public void tearDown() {
    driver.quit();
  }
  @Test
  public void example() {
    driver.get("https://www.pivotaltracker.com/introduction");
    driver.manage().window().setSize(new Dimension(1116, 693));
    driver.findElement(By.xpath("//div[@id=\'main\']/div/div/form/input")).click();
    driver.findElement(By.xpath("//input")).sendKeys("demo");
    driver.findElement(By.xpath("//button[@type=\'submit\']")).click();

  }*/
}
